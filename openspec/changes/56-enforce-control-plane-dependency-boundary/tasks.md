## 1. Reconcile the finalized enforcement handoff

- [ ] 1.1 Verify block 55 is applied and inventory its exact production Standard, Web-only, Internal-worker, and Run-once registration seams, finalized allow/deny catalog, dependency-path reporter, compiled/static checks, factory metadata, and constructor/index/native/geodata/launcher sentinel hooks; stop and report exact missing inputs rather than create parallel roots.
- [ ] 1.2 Compare block 39's processing-only helpers and block 55's migration tests, select the canonical implementation of each guard, and map every existing assertion to the block-56 policy layer before removing any duplication.
- [ ] 1.3 Record the finalized role-owned assembly/project/package closure, including any shared entry/bootstrap assembly; treat an unresolved heavy edge in the Standard/Web-only closure as a blocker rather than exempt the whole dispatcher assembly.
- [ ] 1.4 Enumerate every compiled Web component and production control-plane/hosted/application-service activation root plus every application-owned factory registration to establish complete graph coverage.

## 2. Centralize policy and diagnostics

- [ ] 2.1 Create one typed test-support policy for role applicability, approved activation roots, forbidden heavy categories, required Worker/Run-once categories, exact lightweight contracts, allowed provider scopes, and forbidden project/package/assembly edges using the block-55 handoff data.
- [ ] 2.2 Encode forbidden categories for Overture/GADM resolution/query/download/export/cache mutation, administrative resolution, DuckDB/native bootstrap, geometry/index/prepared-geometry, bundled country-index load, worker handlers, and in-process processing execution without using namespace strings as the sole classifier.
- [ ] 2.3 Encode exact approved control-plane contracts for transport DTOs/job clients, inventory/deletion/reset/control services, configuration/UI state, geometry-free country/profile identity, lazy Npgsql repositories/work detection, skipped-asset SQLite, and bounded inventory/control metadata SQLite; require their complete transitive closure to remain lightweight.
- [ ] 2.4 Implement a deterministic diagnostic record and formatter containing rule id, role/mode, production root or registration owner, forbidden/missing category, offending type/assembly/package, ordered dependency path or edge kind, and remediation hint; aggregate and sort multiple failures.
- [ ] 2.5 Document policy ownership, false-positive triage, exact-entry allowlist requirements, rename/move updates, positive and near-neighbor fixture requirements, and the prohibition on broad namespace/assembly/package suppressions.

## 3. Enforce project, package, source, and compiled closure

- [ ] 3.1 Inspect direct project and package declarations plus resolved restore assets for every Standard/Web-only control-plane assembly and reject forbidden direct or transitive geodata/native edges against the finalized approved manifest.
- [ ] 3.2 Inspect compiled assembly references, base types, generic arguments, constructor signatures, and injectable properties in an isolated metadata/reflection context without instantiating analyzed types or running static/native initialization.
- [ ] 3.3 Enumerate generated Razor component types and their inherited/generated `[Inject]` properties from compiled output so a new page or generated injection is covered automatically; use precise framework/compiler exclusions with self-tests.
- [ ] 3.4 Retain narrow source/project diagnostics for forbidden global Razor imports and stale namespace/type tokens, but prove static enforcement does not depend on fragile string grep when build or reflection metadata is available.
- [ ] 3.5 Add self-contained negative fixtures for direct/transitive project/package/assembly edges, forbidden concrete/namespace categories, generated-style injection, and an intentionally omitted generated/framework exclusion; assert structured diagnostic fields.

## 4. Enforce descriptors, factories, and transitive graphs

- [ ] 4.1 Inspect exact production Standard and Web-only descriptors before provider build, including service and implementation types, instances, lifetimes, keyed/open generics, concrete/hosted aliases, alias identity, and factory registrations.
- [ ] 4.2 Require every application-owned factory to expose analyzable dependency/category metadata, reject unclassified factories without invoking them, and compare declared dependencies with implementation signatures where available.
- [ ] 4.3 Walk constructor parameters, injectable properties, closed generic arguments, aliases, and declared factory dependencies from all compiled Web and control-plane roots with cycle detection and deterministic ordering.
- [ ] 4.4 Assert Standard and Web-only have the same heavy exclusion and approved lightweight closure, with only the finalized scheduling-policy difference, and report complete root-to-forbidden paths.
- [ ] 4.5 Add in-memory negative fixtures for forbidden descriptors, aliases, hosted/open-generic registrations, multi-hop and cyclic constructor paths, missing/dishonest factory metadata, plus an approved factory case; do not mutate production files to test failures.

## 5. Guard lightweight allowances against escape paths

- [ ] 5.1 Add positive policy fixtures proving approved transport DTO/client, inventory/maintenance, UI/configuration, and country/profile identity contracts pass without broad assembly exemptions.
- [ ] 5.2 Pair metadata-only SQLite positives with near-neighbor negatives that query geodata content, count area rows, mutate/export caches, pool geodata handles, or reach geometry/native code.
- [ ] 5.3 Pair lazy Npgsql repository/work-detector positives with near-neighbor negatives that reach a processing executor, geodata resolver, eager connection, or worker implementation.
- [ ] 5.4 Pair bounded country identity/profile positives with spatial country resolver/index negatives, preserving exact alpha2/alpha3/territory contract ownership from block 55.
- [ ] 5.5 Assert allowing an interface, DTO, or provider contract never implicitly allows its implementation project, package, assembly, or undeclared factory closure.

## 6. Add positive Worker and Run-once composition guards

- [ ] 6.1 Define required category manifests for Internal-worker and Run-once from their finalized production entry handlers/executors, administrative resolver, source query/cache/export graph, DuckDB/native ownership, and bundled country-index path.
- [ ] 6.2 Inspect the exact heavy-role descriptors and graph reachability with metadata/fake leaves, asserting required categories are present while Razor, Web inventory, and Web control services are absent; never initialize native/geodata infrastructure.
- [ ] 6.3 Add negative fixtures that remove each required heavy category or inject a Web-only root and prove diagnostics identify the affected role and missing/forbidden category.
- [ ] 6.4 Run the four-role matrix in one policy suite so Standard/Web-only absence and Worker/Run-once completeness cannot drift independently.

## 7. Promote deterministic runtime sentinels

- [ ] 7.1 Consolidate block-55 hooks into instance-scoped throwing/counting sentinels for native/DuckDB bootstrap, pre-country-index load, geodata file open/query/export/cache mutation, in-process executor entry, external database connection, inventory scan where applicable, and worker launch/session creation.
- [ ] 7.2 Build/start exact Standard and Web-only production hosts with fake external boundaries and activate representative compiled page/control-service graphs; assert startup and rendering touch no heavy, database, inventory, or worker-launch sentinel.
- [ ] 7.3 Exercise rejected, busy, unavailable, and pre-admission failure outcomes for Lookup, cache/maintenance, manual processing, and Standard scheduling; assert zero worker sessions and zero local-heavy fallbacks.
- [ ] 7.4 Exercise admitted Lookup, cache/maintenance, manual processing, and Standard detector-positive scheduled paths through a recording fake child boundary; assert exactly one fake session per admission, no real process, and no local-heavy sentinel.
- [ ] 7.5 Use only minimal non-geodata metadata SQLite fixtures where inventory activation is required, assert bounded reads and released handles, and prohibit live PostgreSQL, geodata, download, native library, Kestrel port, timing sleep, process, or RSS dependencies.
- [ ] 7.6 Add hidden-activation and unexpected-launch negative fixtures that prove sentinel failures include the activating owner/category and stable count/order details.

## 8. Place enforcement in fast default CI

- [ ] 8.1 Place policy helpers and focused static-closure, generated-component, descriptor/graph, role-matrix, runtime-sentinel, and self-test classes under `tests/ImmichReverseGeo.Tests/`; assign neither `Integration` nor `Performance`.
- [ ] 8.2 Parse restore/compiled metadata once per immutable input and matrix shared production setup without sharing mutable sentinels; keep negative fixtures in memory and document focused test filters for local iteration.
- [ ] 8.3 Verify the normal `npm run test`/default-runsettings path discovers and executes every boundary layer; add a discovery assertion only if the existing CI command could silently omit the Web test assembly.
- [ ] 8.4 Run focused architecture/composition tests and the full default-exclusion suite without opt-in environment dependencies; investigate a speed regression rather than moving the policy into an excluded category.
- [ ] 8.5 Run `openspec validate 56-enforce-control-plane-dependency-boundary --strict`, inspect final `openspec status --change 56-enforce-control-plane-dependency-boundary`, and review the diff to confirm only MASTERPLAN block 56 and this change's existing artifacts were edited and block 57 was untouched.
