## 1. Bind prerequisite production seams

- [ ] 1.1 Re-read the applied block-55/56 role policy, forbidden constructor/index/native/geodata sentinels, and exact Standard/Web-only production composition; record the landed reusable owners without editing blocks 55–56.
- [ ] 1.2 Re-read block 66's complete 6641 child-memory observation shape and one-second sampling semantics, and block 67's production launcher fixture, process-tree registry, stream/finality waits, controlled cycle modes, and workspace cleanup APIs; stop rather than create a parallel launcher, child, telemetry owner, or production fault seam.
- [ ] 1.3 Define one validated soak configuration for seed, positive warmup/measured counts, ProcessAssets-majority and nonzero CoordinateLookup/CacheMutation integer weights, optional cancellation/failure cycles, fixture profile, output root, and optional external threshold profile.

## 2. Opt-in test selection and outputs

- [ ] 2.1 Categorize every soak case as `Performance`; add `performance.runsettings` selecting Performance only while leaving `.runsettings` and `integration.runsettings` exclusions unchanged.
- [ ] 2.2 Add and document one explicit Web-test-project performance command/task that writes results beneath `_out/performance/worker-memory-soak/test-results` and is not invoked by `npm run test`, `npm run test:integration`, or required CI.
- [ ] 2.3 Create one run-unique `_out/performance/worker-memory-soak/<run-id>/` evidence owner with atomic manifest/record/summary writing and failure retention.
- [ ] 2.4 Add redaction assertions ensuring evidence excludes credentials, arbitrary environment/configuration, coordinates, payloads, raw protocol/stderr, external paths, secrets, and exception text.

## 3. Deterministic production-path workloads

- [ ] 3.1 Add minimal immutable local ProcessAssets database/geodata inputs, deterministic CoordinateLookup cases, and CacheMutation source/candidate/publication fixtures accepted by the landed production seams, with no live network or download/export requirement.
- [ ] 3.2 Add a fail-fast no-network/download/export sentinel and verify all three job kinds run through the exact production worker executable, command builder, launcher, protocol bridge, classifier, and disposal path.
- [ ] 3.3 Generate and record the seeded warmup and measured sequences, validate normalized proportions, keep one production Standard/Web-only control process alive, and reset measured sentinel/memory baselines after warmup.
- [ ] 3.4 Add an optional external full-load local fixture profile without committing large generated data or weakening the no-network contract.

## 4. Per-job finality and structural leak checks

- [ ] 4.1 Register each worker before start, require a fresh PID distinct from every earlier job PID, and record controller/worker identity plus exact job kind, phase, and outcome.
- [ ] 4.2 Before launching the next job, await authoritative protocol/classifier finality, bounded worker termination, both redirected-stream drains, bridge/telemetry completion, launcher/session disposal, process-tree absence, and fixture-handle release.
- [ ] 4.3 Snapshot each isolated workspace and fail on remaining attempt-owned temp, download, candidate, staging, journal, or sidecar artifacts while distinguishing immutable fixture and validated final outputs.
- [ ] 4.4 Keep unconditional process-tree/file cleanup as a safety net but fail and record any fallback kill/delete/recovery, surviving descendant, leaked handle, reused PID, or missing finality.
- [ ] 4.5 Add optional deterministic cooperative-cancellation and controlled pre-publication failure cycles using existing block-67 controls; require fresh PIDs, identical finality gates, authoritative outcomes, and no automatic retry.

## 5. Control-plane and memory evidence

- [ ] 5.1 Wire landed block-55/56 counting sentinels into the long-lived Web control process and fail immediately on any measured forbidden constructor/factory, country-index, native/DuckDB, geodata open/query/export/mutation, or in-process execution observation.
- [ ] 5.2 Capture monotonic-time-stamped Web working-set observations at phase/job boundaries and consume block-66 child available/unavailable observations without adding a second child sampler.
- [ ] 5.3 Emit sample counts and explicit caveats that one-second sampling can leave short workers with start/finality observations only and never claims OS peak, process-tree, cgroup, container, or system memory.
- [ ] 5.4 Report per-kind/phase counts, min/median/max where available, first/last deltas, missing-sample reasons, and trend descriptions without any default numeric RSS/delta/slope failure.
- [ ] 5.5 Add explicit external host-RSS and Linux cgroup-v2 profile parsing, provenance and capability matching, units/scope/warmup/minimum-sample rules, and applied/not-applied threshold decisions; enable numeric failure only for a compatible selected profile.

## 6. Evidence and acceptance tests

- [ ] 6.1 Emit the resolved configuration/capability manifest, ordered job/PID/outcome/finality records, process-tree/stream/handle evidence, artifact snapshots, sentinel counts, raw sourced memory observations, trend/profile summary, and test results under the run directory.
- [ ] 6.2 Add a short reproducible validation profile proving all three job kinds, warmup reset, fresh PIDs, launch barriers, zero structural leaks, zero Web sentinels, redacted artifacts, and unprofiled diagnostic-only memory trends.
- [ ] 6.3 Add focused negative harness tests for duplicate PID evidence, missing termination/drain/disposal, fallback orphan cleanup, surviving descendants, leaked candidate/temp artifacts or handles, no-network sentinel activation, and Web heavy-constructor activation.
- [ ] 6.4 Add profile tests for compatible host-RSS/cgroup-v2 numeric decisions and incompatible/unavailable profiles that remain not-applied while structural assertions stay mandatory.

## 7. Verification and scoped handoff

- [ ] 7.1 Run the focused short Performance profile and explicit performance command; inspect one success bundle and retained failure bundle beneath `_out/performance/worker-memory-soak/`.
- [ ] 7.2 Run `npm run test` and `npm run test:integration` selection checks and prove Performance remains excluded from both; run no live Overture/GADM access or downloads.
- [ ] 7.3 Run `openspec validate 68-add-repeated-worker-memory-soak --strict`, confirm OpenSpec status is 4/4 complete, and review a scoped diff proving only block 68 planning and its later Performance test/fixture/runsettings/task-runner/maintainer assets changed—never blocks 67 or 69 or production contracts.
- [ ] 7.4 Document the optional `_out` manifest and full-load/cgroup profile handoff that block 69 may consume later without editing workflows or making CI integration a block-68 acceptance condition.
