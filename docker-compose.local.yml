services:
  immich-reversegeo:
    build:
      context: .
      dockerfile: src/ImmichReverseGeo.Web/Dockerfile
    image: immich-reversegeo:local
    ports:
      - "8080:8080"
    volumes:
      - reversegeo-config:/config
      - reversegeo-data:/data
    env_file:
      - .env
    environment:
      - ASPNETCORE_URLS=http://+:8080
    # Keep in step with docker-compose.yml — gives the .NET GC a ceiling to collect against.
    mem_limit: 2g
    restart: unless-stopped

volumes:
  reversegeo-config:
  reversegeo-data:
